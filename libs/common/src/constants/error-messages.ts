export const INVALID_CREDENTIALS = `Login or password are invalid`;
export const FORBIDDEN = `You don't have enaugh permissions`;
export const INVALID_TOKEN = `Token is invalid`;
export const NOT_FOUND = (entity: string) => `${entity} not found`;
export const ALREADY_EXIST = (entity: string) => `${entity} already exists`;
